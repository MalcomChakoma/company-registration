spring:
  datasource:
    url: jdbc:mysql://localhost:3306/companyReg?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: P@55w0rd@123
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      minimum-idle: 2
      maximum-pool-size: 10
      idle-timeout: 30000
      connection-timeout: 30000
      validation-timeout: 5000

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update   # use 'validate' in production
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  jackson:
    serialization:
      indent_output: true

############################################################
# LOGGING
############################################################
logging:
  level:
    root: INFO
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG